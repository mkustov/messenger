<%= simple_form_for(@user, url: user_path, method: :post, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.input :username %>
  <%= f.input :dialect, collection: User::DIALECTS %>
  <%= f.submit %>
<% end %>
